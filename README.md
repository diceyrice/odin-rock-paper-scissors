# odin-rock-paper-scissors

This project is complete as part of The Odin Project and is a website where rock paper scissors is to be played, entirely in the console, using JavaScript. This project assumes that the player inputs a valid choice, so cases where the user inputs anything else than "rock, "paper," or "scissors" (capitalization is accounted for) will cause an error.